site_name: Calculator Docs
nav:
  - Главная: index.md
  - Установка: installation.md
  - API документация:
      - API референс: api-reference.md
      - Полное API: api.md

theme:
  name: material

plugins:
  - search
  - mkdocstrings:
      default_handler: python
      handlers:
        python:
          options:
            show_root_heading: true
            show_source: true
            show_category_heading: true
            heading_level: 3
            docstring_style: google
            separate_signature: true
            line_length: 80
            # 👇 ВАЖНО: Настройки для примеров
            show_examples: true
            examples_as_code: true # Показывать примеры как блоки кода
            merge_init_into_class: true
            # 👇 Дополнительные настройки для красоты
            members_order: source
            group_by_category: false
            filters: ["!^_", "!^__"] # Только публичные методы

markdown_extensions:
  - admonition
  - codehilite
  - toc:
      permalink: true
  - pymdownx.superfences
