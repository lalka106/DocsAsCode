site_name: Calculator Docs
nav:
  - Главная: index.md
  - Установка: installation.md
  - API документация:
      - API референс: api-reference.md
      - Полное API: api.md

theme:
  name: material

plugins:
  - search
  - mkdocstrings:
      default_handler: python
      handlers:
        python:
          options:
            show_root_heading: true
            show_source: true
            show_category_heading: true
            heading_level: 3
            docstring_style: google
            separate_signature: true
            line_length: 80
            # 👇 ВМЕСТО show_examples используем show_signature_annotations
            show_signature_annotations: true
            merge_init_into_class: true
            members_order: source
            group_by_category: false
            filters: ["!^_", "!^__"]
            # 👇 Примеры теперь включаются через docstring_style
            summary: false

markdown_extensions:
  - admonition
  - codehilite
  - toc:
      permalink: true
  - pymdownx.superfences
